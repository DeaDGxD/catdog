import re
def finder(word):
    tweets = open(##path to file##).readlines()
    car = 0
    for i in tweets:
        o = re.compile(word)
        f = o.findall(i)
        for g in f:
            car = car + 1
    return car
