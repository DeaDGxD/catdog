import re
def finder(word):
    stuff = open(##path to file##).readlines()
    car = 0
    for i in stuff:
        o = re.compile(word)
        f = o.findall(i)
        for g in f:
            car = car + 1
    return car
